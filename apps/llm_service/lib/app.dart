import 'package:flutter/material.dart';

class LLMServiceApp extends StatelessWidget {
  const LLMServiceApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('LLM Service App'),
        ),
      ),
    );
  }
} 