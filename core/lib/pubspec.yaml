name: core
description: Core module for SuoKe Life project
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.28.0
  dio: ^5.7.0
  get_it: ^8.0.3
  logger: ^2.5.0  
  injectable: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  injectable_generator: ^2.5.0

flutter:
  uses-material-design: true
