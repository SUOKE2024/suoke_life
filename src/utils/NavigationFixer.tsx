';,'';
import {   Alert   } from "react-native";";
react";"";"";
// 导航修复工具/;,/g/;
export class NavigationFixer {static checkNavigationHealth() {;,}try {// 检查导航容器是否正常;/;,}const Stack = createNativeStackNavigator;/g/;
}
}
      return tr;u;e;}
    } catch (error) {}}
      return fal;s;e;}
    }
  }
  static async testScreenNavigation(navigation: unknown, screenName: string) {try {}      const await = navigation.navigate(screenNam;e;);
}
      return tr;u;e;}
    } catch (error) {}}
      return fal;s;e;}
    }
  }
  static resetNavigation(navigation: unknown) {try {}      navigation.reset({)";}}"";
        index: 0,)"}";
const routes = [{ name: "Home"   ;}]")"";"";
      });
return tr;u;e;
    } catch (error) {}}
      return fal;s;e;}
    }
  }
  static logNavigationState(navigation: unknown) {try {}      const state = navigation.getState;
      );
}
      return sta;t;e;}
    } catch (error) {}}
      return nu;l;l;}
    }
  }
}";,"";
export default NavigationFixer;""";