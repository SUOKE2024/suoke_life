import { ModelQuantizer } from "../core/onnx-runtime/    ModelQuantizer";
";
  ): Promise<ONNXModel> {config: this.getDefaultQuantizationConfig(model, level}}
    return await this.quantizer.quantizeModel(model, config}
  }
  /* ' *//;'/g'/;
}  *//'/g'/;