apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: suoke
  labels:
    app: api-gateway
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: api-gateway