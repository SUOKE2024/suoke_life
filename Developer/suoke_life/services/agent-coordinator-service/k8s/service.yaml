apiVersion: v1
kind: Service
metadata:
  name: agent-coordinator
  namespace: suoke
  labels:
    app: agent-coordinator
    tier: ai
    part-of: suoke-life
spec:
  selector:
    app: agent-coordinator
  ports:
  - name: http
    port: 80
    targetPort: 3007
    protocol: TCP
  type: ClusterIP