/**
 * 模型索引文件
 */
const userModel = require('./user.model');
const profileModel = require('./profile.model');
const healthProfileModel = require('./health-profile.model');

module.exports = {
  userModel,
  profileModel,
  healthProfileModel
}; 