import swaggerJSDoc from "swagger-jsdoc"; import { version } from "../../package.json"; const options: swaggerJSDoc.Options = { definition: { openapi: "3.0.0", info: { title: "小克服务 API", version, description: "索克生活平台供应链与农产品服务智能体API文档", contact: { name: "SUOKE.LIFE开发团队", email: "dev@suoke.life", url: "https://suoke.life" }, }, servers: [ { url: process.env.NODE_ENV === "production" ? "https://api.suoke.life" : "http://localhost:3011", description: process.env.NODE_ENV === "production" ? "生产服务器" : "开发服务器", }, ], components: { securitySchemes: { bearerAuth: { type: "http", scheme: "bearer", bearerFormat: "JWT", }, }, }, security: [{ bearerAuth: [] }], }, apis: ["./src/routes/**/*.ts", "./src/models/**/*.ts"], }; const swaggerSpec = swaggerJSDoc(options); export default swaggerSpec;
