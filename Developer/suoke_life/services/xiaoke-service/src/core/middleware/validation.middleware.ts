import { Request, Response, NextFunction } from "express"; import Joi from "joi"; import { logger } from "../../utils/logger"; export const validateRequest = (schema: Joi.ObjectSchema) => { return (req: Request, res: Response, next: NextFunction) => { const { error } = schema.validate(req.body); if (error) { logger.warn(`请求验证失败: ${error.message}`, { path: req.path, body: req.body, error: error.details }); return res.status(400).json({ success: false, message: error.details[0].message, error: "VALIDATION_ERROR", }); } next(); }; }; export const validateQuery = (schema: Joi.ObjectSchema) => { return (req: Request, res: Response, next: NextFunction) => { const { error } = schema.validate(req.query); if (error) { logger.warn(`查询参数验证失败: ${error.message}`, { path: req.path, query: req.query, error: error.details }); return res.status(400).json({ success: false, message: error.details[0].message, error: "VALIDATION_ERROR", }); } next(); }; }; export const validateParams = (schema: Joi.ObjectSchema) => { return (req: Request, res: Response, next: NextFunction) => { const { error } = schema.validate(req.params); if (error) { logger.warn(`路径参数验证失败: ${error.message}`, { path: req.path, params: req.params, error: error.details }); return res.status(400).json({ success: false, message: error.details[0].message, error: "VALIDATION_ERROR", }); } next(); }; };
