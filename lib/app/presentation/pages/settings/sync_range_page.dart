import 'package:flutter/material.dart';
import '../../core/base/base_page.dart';

class SyncRangePage extends BasePage {
  const SyncRangePage({Key? key}) : super(key: key);

  @override
  String get title => '同步范围设置';

  @override
  Widget buildBody(BuildContext context) {
    return Container(
      // 页面内容
    );
  }
} 