import 'package:flutter/material.dart';

class LifeServiceApp extends StatelessWidget {
  const LifeServiceApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Life Service App'),
        ),
      ),
    );
  }
} 